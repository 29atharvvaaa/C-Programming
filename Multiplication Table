#include <stdio.h>
int main() {
    int n,i,p;
    printf("Enter a Number:\n");
    scanf("%d", &n);
    printf("Multiplication Table of %d is:\n", n);
    for(i=1; i<=10; i++) {
        p = n * i;
        printf("%d\n", p);
    }
    return 0;
}
